[package]
name = "strata-engine"
description = "GPU vector renderer (wgpu + lyon) with cosmic-text and a tiny-skia CPU fallback. Native + wasm32 backends."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lints]
workspace = true

# Heavy deps (wgpu/lyon/cosmic-text/tiny-skia) land in task 0.6 once the
# render spike (0.2) has committed the interop approach via ADR.
