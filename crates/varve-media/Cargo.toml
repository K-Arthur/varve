[package]
name = "varve-media"
description = "Animated image decoding: GIF, APNG, and WebP frame extraction with bounded allocation."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
base64 = "0.22"
gif = "0.14"
png = "0.18"
image-webp = "0.2"

[lints]
workspace = true
