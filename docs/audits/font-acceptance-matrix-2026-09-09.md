# Font acceptance matrix — 2026-09-09

Updated 2026-09-13. This preserves the original 24 acceptance scenarios, in their
original order. The earlier matrix substituted narrower unit checks and marked
some end-to-end capabilities implemented without sufficient evidence. Those
completion labels are superseded. A component test is evidence for that component,
not certification of a full scenario.

| # | Original acceptance scenario | Status | Evidence / next executable check |
| --- | --- | --- | --- |
| 1 | A bundled font appears, previews, applies, renders identically on main thread and worker, exports, and survives reopen offline. | Open | Three required licensed variable-font artifacts verify metrics, axes and original-byte identity; offline render/export/reopen oracle pending. |
| 2 | Desktop enumerates an installed family with several faces; selecting a face preserves its exact identity, weight, style, and stretch after restart. | Partial | Native enumeration now returns an artifact hash, collection member, portable face key, and versioned opaque handle; `load_system_font` validates the handle against a fresh collection and the original bytes. Native restart and selected-face rendering proof remain pending. See the September 14 native discovery evidence. |
| 3 | Browser local-font access succeeds, is denied, is unsupported, and is revoked; each state has a clear next action. | Partial | The explicit Allow/Refresh action now distinguishes successful access, permission denial, unsupported API, and runtime error while retaining the compatibility list; revocation and native proof remain pending. |
| 4 | Two different font files with the same family name coexist. The selected one remains selected across save/reopen and the other is not removed by uninstall. | Open | Hash model exists; native family storage and runtime aliases still collapse artifacts. |
| 5 | A TTC/OTC exposes distinct members and selecting one does not collapse to the first file member. | Open | Header/offset checks exist; WOFF2 collection-entry identity now matches single-face parsing, and native enumeration now resolves a member handle per face instead of reusing the first path-level handle. Independent multi-member fixtures, picker/member import and reopen proof pending. See the September 14 parser/native collection evidence. |
| 6 | A variable font named instance and custom axes render, copy/paste, collaborate, export, undo/redo, and reopen without clamping drift. | Partial | The full browser now exposes only the selected family's parsed `fvar` axes with min/default/max bounds, a bounded step, live specimen preview, Reset, and an explicit Use face commit. Persistence, shaping/export parity, clipboard/collaboration, and reopen round trips remain pending. |
| 7 | Real and unavailable weights/styles produce accurate controls; synthetic styling is blocked or clearly disclosed according to product policy. | Partial | Registry-backed weight options now drive the inspector, contextual/floating toolbars, and Logo panel. Static faces expose registered weights scoped to the authored artifact (including collection members) when identity data is available and move the portable member reference with a cross-member weight change; variable faces expose their declared `wght` range; stale legacy values remain visible but disabled, and the floating Bold action is blocked when 700 is unavailable. The floating toolbar and inspector style control now disable Italic unless the selected face/member exposes an italic sibling or declared `ital` axis, while an existing italic request can still be turned off; disabled controls expose a recovery reason and variable italic changes couple the `ital` axis. Axis lookup is exact-face scoped, so a variable sibling cannot affect a static collection member that shares the family and artifact. The floating toolbar, inspector, and Logo style controls move an exact reference to a same-artifact sibling or clear it when the requested style cannot be proven. Style-specific native proof and full missing-face recovery remain pending. See the September 13 toolbar face-policy and final visual evidence. |
| 8 | Opening or hovering the picker creates no undo entry. Escape restores the exact prior formatting. Click creates one entry. | Open | Escape/portal repair and 27 inspected theme/DPR/narrow captures in `e4508d4e7`; typing-to-toolbar transaction separation and inspected Undo/Redo captures in `c2bf35d70`. Current-token contrast and 45 inspected captures committed in `e03dffb3f`; browser regressions in `d71c904fe`. September 12 repeats Undo/Redo after clipping the editing surface to the canvas. Presentation-only preview and range-wide transaction proof pending. |
| 9 | Formatting a substring changes only that range. Formatting several layers changes each intended property without flattening their other rich runs. | Partial | Run splitting/insertion preserve inherited style links in `747d120d4`. The floating, contextual, and inspector family/weight/style/size/line-height/spacing controls now route through the shared range/caret adapter (`65a01e39b`, `85115ec2e`, and the September 13 frontend evidence); focused inspector coverage formats one selected run and keeps the adjacent run and node defaults intact. Inspector multi-layer rich-run coverage and the full layout/reflow oracle remain pending. |
| 10 | A font change recomputes wrapping, autosize, overset, caret, selection, path text, and layout exactly once per logical commit. | Open | Long-text editing no longer repeats whole-paragraph segmentation per glyph label; `dabfbabf1` also contains the editing surface within the canvas. Exact instance geometry and logical-commit oracle pending. |
| 11 | A dynamically downloaded/project font renders on the worker or causes a synchronous main-thread path; no stale or fallback bitmap is reused. | Partial | Blob CSS bridges carry exact face keys and revisions, and the render pipeline now uses the same synchronous worker-font admission predicate for pruning and dispatch. A pending or failed adoption acknowledgement therefore stays on the authoritative main-thread path. Revision-specific worker adoption with real bytes and the main/worker pixel oracle remain pending. |
| 12 | A missing family, missing face, missing glyph, corrupt file, version mismatch, and restricted font each produce a distinct state and recovery action. | Open | Resolver and Missing Fonts UI retain distinct status labels and exact recovery-row keys; exact references with a PostScript name rank a matching installed face before family fallbacks; family-only legacy records and same-family exact artifacts no longer share recovery metadata. Catalog downloads cannot prove the original artifact hash, so exact requests now require an explicit Install and replace face action after installation. Complete capability/preflight and recovery actions for every status remain pending. |
| 13 | Missing-font replacement covers top-level text, rich runs, linked stories, and styles in one undo transaction; original references remain recoverable. | Open | Missing-font dialog and the shared replacement adapter cover top-level/rich/style updates with manifest provenance; linked-story one-step and full recovery assertions remain pending. |
| 14 | Restoring an exact original font later does not silently rewrite the document; the user previews and confirms restoration. | Partial | `b4d90896b` adds an exact-provenance Restore action with a modal current→original preview, affected character/layer counts, wrapping warning, Escape/cancel boundary, and one grouped restore transaction. Ambiguous family-only history stays disabled; full geometry/layout preview and linked-story recovery remain pending. |
| 15 | Select by Font respects current-canvas scope, inherited/run-level fonts, hidden/locked policy, and explicit cross-page navigation. | Partial | Current-page/document scope, effective rich-run/style usage, hidden/locked filtering, exact-face rows, Select, scoped Replace, and an explicit Go to action that activates publishing pages and centers the first match are implemented. Component-instance usage now follows effective variant visibility/text in `buildDocumentFontUsage`, with focused unit coverage and a passing browser Document Fonts workflow; nested component/instance navigation and full cross-page E2E evidence remain pending. |
| 16 | Fontsource install shows progress, cancels cleanly, retries after offline failure, verifies the artifact, survives restart, and removes only the chosen face. | Partial | September 11: 35 focused tests cover stale attempts, two-attempt concurrency, transport deadlines and real SHA-256 verification. The browser storage suite now also proves an interrupted legacy migration retries on the next open; durable restart, exact uninstall, storage integrity and offline E2E remain pending. |
| 17 | An Arabic/Devanagari/CJK/emoji fixture reports coverage accurately and preserves mandatory shaping, grapheme caret behavior, BiDi order, and fallback provenance. | Open | Parser coverage fixes exist; multilingual shaping and caret oracle pending. |
| 18 | A color font renders or reports a precise unsupported-path state; it never silently becomes monochrome in only one renderer. | Open | Color format metadata exists; per-render-path capability and parity proof pending. |
| 19 | Import and clipboard tests preserve family, face, axes, features, rich runs, and missing-font provenance for every supported format. | Open | Optional fontReference schema exists; per-format/rich-run dependency closure evidence pending. |
| 20 | PDF/SVG/package export either embeds, subsets, outlines, rasterizes, or blocks according to the explicit user choice and verified policy, with no false success. | Partial | Export requests now retain exact face references; verified-byte preflight blocks missing exact faces and refuses a family-addressed export that would embed one same-family artifact for multiple exact runs. Readiness timeout is an error. Native per-face embedding/outline wire support and full policy verification remain pending. See the September 14 export preflight evidence. |
| 21 | The picker handles thousands of families within declared latency/memory budgets and remains keyboard-accessible under virtualization. | Open | Compact picker active-option behavior, explicit listbox semantics, Alt+ArrowDown opening, focus restoration, anchor-width menu sizing, and a bounded pre-measure bootstrap (120 estimated rows with the active option retained) are covered by the focused selector suite on 2026-09-13; the full browser uses measured overscan virtualization (`2b7d98ff9`), with 1k/10k measured budgets still pending. |
| 22 | Image font identification uses the selected crop, handles cancel and low confidence, previews candidates, and applies to an explicit text target or creates editable text. | Partial | `FontDetectSection` uses the existing canvas crop tool, follows crop/rotation/flip settings during bounded extraction, offers a full-image fallback, aborts pending decode work on Cancel/target change, supplies live registry/local comparison dependencies, and queues **Use for new text** with an optional exact reference. An explicit existing-text target picker now applies a reviewed candidate to only that layer in one undo transaction and preserves exact identity when available. When both OCR assets are already installed, **Read text with local OCR** runs the existing bounded pipeline, fills an editable recognized-text field, renders selectable OCR boxes over the bounded preview, and limits comparison to the selected regions. Candidate cards show the chosen target preview; low-confidence, insufficient-quality, and out-of-catalogue results require an explicit preview review before **Apply to target**. Native model-present captures, candidate overlays with real assets, and OCR box selection on Tauri remain pending. |
| 23 | Save/reopen, undo/redo, autosave recovery, document switching, and two simultaneous documents do not leak project fonts or picker state across documents. | Partial | The migration journal now retries an interrupted browser migration on a later database open; document lifetime isolation, autosave recovery, and two-document picker-state E2E remain pending. |
| 24 | Installing or removing an OS font while the app runs refreshes or exposes an explicit Refresh action without corrupting the current document. | Partial | The browser and desktop surfaces expose an explicit Refresh/Allow action, and native enumeration now rechecks the current collection when reading a handle. OS watcher/automatic refresh and native document-preservation proof remain pending. |

## Evidence ownership

Linux Chromium owns browser interaction, canvas and screenshot evidence. Linux
Tauri/WebKitGTK owns native enumeration, exact-file reads and restart checks.
Windows WebView2 and macOS WKWebView need native CI/manual runs on those hosts;
the next check is the font-specific embedded WDIO spec once added. Portable
collaboration payloads are in scope; live transport is excluded.

See the [dated audit](./font-system-audit-2026-09-09.md) for historical observations
and the [remaining-work plan](../plans/font-system-remaining-2026-09-10.md) for
implementation order and gate ownership.

Inspector and website captures, validation failures and remaining platform checks
are in the [integration evidence log](./font-integration-evidence-2026-09-10.md).

Required fixture provenance, the corrected OS/2 audit finding, and the reproduced
WOFF2 identity mismatch are in the [parser evidence log](./font-os2-metrics-evidence-2026-09-10.md).
These bounded checks do not close any full acceptance scenario.

Current token/layout measurements, download cancellation and timeout regressions,
and the inspected website help are recorded in the
[September 11 continuation](./font-lifecycle-evidence-2026-09-11.md).

Long-text responsiveness, editing-surface hit testing, four inspected captures,
compiler limits and explicit frontend delivery obligations are recorded in the
[September 12 frontend checkpoint](./font-frontend-evidence-2026-09-12.md).
