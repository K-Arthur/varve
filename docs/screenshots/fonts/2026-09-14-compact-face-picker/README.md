# Compact face picker captures — 2026-09-14

These inspected captures come from the Chromium DPR 1 run of
`tests/e2e/canvas/font-toolbar-visual.spec.ts` at 1280×800 CSS pixels. The
family menu is expanded on IBM Plex Sans Variable so the registered 400, 500,
600, and 700 rows are visible after the viewport scrolls to the first face.

- `light-faces-open.png` — light theme
- `dark-faces-open.png` — dark theme
- `high-contrast-faces-open.png` — high-contrast theme

The same test also checks containment, 32px control heights, shared gaps and
padding, focus/active-descendant behavior, and the narrow viewport path.
