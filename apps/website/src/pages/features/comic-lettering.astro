---
import Button from '../../components/Button.astro';
import Layout from '../../layouts/Layout.astro';
import StatusPill from '../../components/StatusPill.astro';
import { sitePath } from '../../lib/siteUrl';
---

<Layout
  title="Comic & Webtoon Lettering"
  description="Varve keeps comic lettering editable: reusable balloon recipes, tails, captions, sound effects, text-aware fitting, and webtoon-ready page structure."
>
  <div class="container-custom">
    <div class="feature-page comic-page">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <a href={sitePath('/features')}>Features</a> / Comic &amp; Webtoon Lettering
      </nav>

      <div class="comic-page__kicker"><StatusPill status="partial" label="Built foundation" /></div>
      <h1>Letter the page without flattening it.</h1>
      <p class="feature-intro">
        Varve treats a balloon as a small editable composition: ordinary text, an ordinary body,
        and one or more ordinary tails held together by a reversible recipe. Dialogue wraps to the
        shape of the balloon, tails bend and aim, and the words stay selectable text — in the same
        document, with the same typography, layers, selection, and export systems.
      </p>

      <figure class="comic-shot">
        <img
          src={sitePath('/screenshots/comic-lettering-light.png')}
          alt="A speech balloon selected in Varve with a long dialogue line stacked into a balloon contour; the Comic balloon inspector shows tail endpoint, curve and base width, line shape, fit policy, and add/flip/remove tail actions"
          width="1280"
          height="720"
          loading="lazy"
          decoding="async"
        />
        <figcaption>
          Four wrapped lines with the longest line mid-balloon, and the Comic balloon inspector:
          fit policy, line shape, and editable tails.
        </figcaption>
      </figure>

      <section class="comic-visual" aria-label="Editable comic lettering example">
        <div class="comic-visual__panel">
          <span class="comic-visual__caption">PAGE 01 · CITY BLOCK</span>
          <div class="comic-visual__sun" aria-hidden="true"></div>
          <div class="comic-visual__line comic-visual__line--one" aria-hidden="true"></div>
          <div class="comic-visual__line comic-visual__line--two" aria-hidden="true"></div>
          <div class="comic-balloon comic-balloon--speech">
            <span>We can still make the last train.</span>
            <i aria-hidden="true"></i>
          </div>
          <div class="comic-balloon comic-balloon--caption">
            <span>Meanwhile, above the station...</span>
          </div>
          <div class="comic-sfx" aria-label="Sound effect text">CLACK</div>
          <div class="comic-visual__baseline" aria-hidden="true"></div>
        </div>
        <div class="comic-visual__legend">
          <div><span class="legend-dot legend-dot--teal"></span><strong>Text stays text</strong><span>Edit the words, font, language, and writing mode in place.</span></div>
          <div><span class="legend-dot legend-dot--sandstone"></span><strong>Wrapping follows the shape</strong><span>Round balloons narrow their first and last lines so the stack echoes the outline.</span></div>
          <div><span class="legend-dot legend-dot--terracotta"></span><strong>Tails bend and aim</strong><span>Drag the endpoint, curve it, widen it, flip it, or give one balloon two tails.</span></div>
        </div>
      </section>

      <section class="feature-section">
        <h2>What it is</h2>
        <p>
          Comic lettering builds on Varve's existing text, strokes, effects, warp, group, layer,
          page, and export foundations. It does not create a separate Comic workspace or a second
          text engine. A speech balloon is a selected group with a shared recipe, so body and tail
          nodes remain available for ordinary editing and the source text remains available for
          localization. Fitting never reduces the font size; overflow is a visible state you can
          act on.
        </p>
      </section>

      <section class="feature-section">
        <h2>Designed around real lettering problems</h2>
        <div class="comic-problems">
          <article>
            <h3>Words should not disappear at the edge</h3>
            <p>Fit policy is explicit: reflow in the balloon, fit the balloon to text, or show overflow. A long translation reports overflow instead of quietly shrinking type, and one command re-fits it.</p>
          </article>
          <article>
            <h3>Dialogue should not run in a ribbon</h3>
            <p>Wrapping a single-line text node into a balloon caps the measure and stacks the dialogue. The first and last lines narrow so the text mass follows a round balloon, with the longest line near the middle.</p>
          </article>
          <article>
            <h3>Tails should be part of the balloon</h3>
            <p>Tails are ordinary nodes with a curve, a base width, a flip, and a remove. Thought balloons draw the conventional chain of decreasing circles, and the tail tip stays put when the body is fitted.</p>
          </article>
        </div>
      </section>

      <section class="feature-section">
        <h2>Key capabilities</h2>
        <ul>
          <li><strong>Balloon recipes:</strong> speech, thought, caption, whisper, and shout treatments built from editable scene nodes</li>
          <li><strong>Contour-aware wrapping:</strong> line widths derived from the number of lines the dialogue forms, shared by selection, editing, paint, and export</li>
          <li><strong>Tail editing:</strong> one or more tails, an editable endpoint, curve and base width, flip, remove, and a detach action for deliberate freeform edits</li>
          <li><strong>Text-aware fitting:</strong> shared text geometry reports fit, near-overflow, or overflow without mutating the document, and fitting never shrinks the type</li>
          <li><strong>Sound-effect lettering:</strong> reuse the existing text stroke, effect, warp, and transform systems instead of creating a raster-only SFX layer; strokes follow wrapped lines</li>
          <li><strong>Page structure:</strong> comic, manga, and webtoon-vertical starting profiles with page order, panel metadata, reading direction, and bounded slice planning</li>
          <li><strong>Accessible review:</strong> named controls, keyboard-reachable Inspector actions, and status text that explains why a balloon needs attention</li>
        </ul>
      </section>

      <section class="feature-section limitations">
        <h2>Honest status</h2>
        <p>
          The editable balloon foundation is in the beta. Speaker-target placement, joined balloons
          with shared outlines, a canvas draw-a-balloon tool, vertical contour columns, and full
          publisher upload automation are intentionally not presented as complete. Webtoon and
          Tapas limits remain dated, source-linked export guidance rather than hard-coded platform
          promises.
        </p>
        <p>
          See the <a href={sitePath('/docs/tools/typography')}>typography guide</a> for the shared
          text model and the <a href={sitePath('/docs/file-formats')}>file-format guide</a> for
          document dependencies and export boundaries.
        </p>
      </section>

      <div class="feature-cta">
        <h2>Keep the lettering alive.</h2>
        <Button href={sitePath('/download')}>Download Varve</Button>
        <Button variant="secondary" href={sitePath('/features/typography')}>Explore Typography</Button>
      </div>
    </div>
  </div>
</Layout>

<style>
  .comic-page__kicker {
    margin-bottom: 0.75rem;
  }

  .comic-page h1 {
    max-width: 680px;
  }

  .comic-shot {
    margin: -0.5rem 0 2.5rem;
    display: grid;
    gap: 0.6rem;
  }

  .comic-shot img {
    width: 100%;
    height: auto;
    border: 1px solid var(--border-default);
    border-radius: var(--website-radius-card);
    box-shadow: var(--elevation-shadow-raised);
  }

  .comic-shot figcaption {
    color: var(--text-tertiary);
    font-size: 0.8rem;
  }

  .comic-visual {
    margin: -0.5rem 0 3rem;
    display: grid;
    gap: 1rem;
  }

  .comic-visual__panel {
    position: relative;
    min-height: 390px;
    overflow: hidden;
    border: 1px solid var(--border-default);
    border-radius: var(--website-radius-card);
    background:
      linear-gradient(155deg, transparent 0 58%, color-mix(in oklab, var(--brand-teal) 12%, transparent) 58% 59%, transparent 59%),
      linear-gradient(20deg, transparent 0 67%, color-mix(in oklab, var(--brand-terracotta) 11%, transparent) 67% 68%, transparent 68%),
      var(--surface-muted);
    box-shadow: var(--elevation-shadow-raised);
  }

  .comic-visual__caption {
    position: absolute;
    top: 1.25rem;
    left: 1.5rem;
    color: var(--text-tertiary);
    font-size: 0.7rem;
    letter-spacing: 0.14em;
  }

  .comic-visual__sun {
    position: absolute;
    top: 4.5rem;
    right: 16%;
    width: 6rem;
    height: 6rem;
    border-radius: 50%;
    background: color-mix(in oklab, var(--brand-sandstone) 50%, var(--surface-muted));
  }

  .comic-visual__line {
    position: absolute;
    bottom: 4rem;
    width: 72%;
    height: 1px;
    background: var(--border-strong);
    transform-origin: left;
  }

  .comic-visual__line--one { left: 0; transform: rotate(-8deg); }
  .comic-visual__line--two {
    left: 32%;
    bottom: 7rem;
    transform: rotate(9deg);
    background: color-mix(in oklab, var(--border-strong) 65%, transparent);
  }

  .comic-visual__baseline {
    position: absolute;
    right: 9%;
    bottom: 3.2rem;
    width: 30%;
    height: 5rem;
    border-top: 3px solid color-mix(in oklab, var(--text-primary) 72%, transparent);
    border-right: 3px solid color-mix(in oklab, var(--text-primary) 72%, transparent);
    transform: skewX(-18deg);
  }

  .comic-balloon {
    position: absolute;
    display: grid;
    place-items: center;
    padding: 1.1rem 1.5rem;
    color: var(--text-primary);
    background: var(--surface-card);
    border: 2px solid var(--text-primary);
    box-shadow: 0 0.6rem 0 color-mix(in oklab, var(--brand-teal) 14%, transparent);
    font-family: var(--font-interface);
    font-weight: 650;
    text-align: center;
  }

  .comic-balloon--speech {
    top: 6.5rem;
    left: 10%;
    width: min(48%, 330px);
    min-height: 100px;
    border-radius: 50%;
  }

  .comic-balloon--speech i {
    position: absolute;
    right: 12%;
    bottom: -2.1rem;
    width: 2.2rem;
    height: 3.2rem;
    background: var(--surface-card);
    border-right: 2px solid var(--text-primary);
    border-bottom: 2px solid var(--text-primary);
    clip-path: polygon(0 0, 100% 0, 62% 100%);
    transform: rotate(18deg);
  }

  .comic-balloon--caption {
    top: 18rem;
    right: 10%;
    padding: 0.9rem 1.1rem;
    border-radius: 0.25rem;
    border-color: var(--brand-terracotta);
    font-family: var(--font-editorial);
    font-weight: 600;
  }

  .comic-sfx {
    position: absolute;
    right: 18%;
    bottom: 7.3rem;
    color: var(--brand-terracotta);
    font-family: var(--font-editorial);
    font-size: clamp(2rem, 7vw, 3.6rem);
    font-weight: 800;
    letter-spacing: 0.06em;
    transform: rotate(-9deg) skewX(-10deg);
    text-shadow: 0.06em 0.06em 0 var(--surface-card), 0.11em 0.11em 0 var(--text-primary);
  }

  .comic-visual__legend {
    display: grid;
    gap: 0.7rem;
    padding: 1.1rem 1.25rem;
    border: 1px solid var(--border-default);
    border-radius: var(--website-radius-card);
    background: var(--surface-card);
  }

  .comic-visual__legend > div {
    display: grid;
    grid-template-columns: 0.65rem 10rem 1fr;
    gap: 0.7rem;
    align-items: baseline;
    color: var(--text-secondary);
    font-size: 0.875rem;
  }

  .comic-visual__legend strong { color: var(--text-primary); }

  .legend-dot { width: 0.6rem; height: 0.6rem; border-radius: 50%; }
  .legend-dot--teal { background: var(--brand-teal); }
  .legend-dot--sandstone { background: var(--brand-sandstone); }
  .legend-dot--terracotta { background: var(--brand-terracotta); }

  .comic-problems {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .comic-problems article {
    padding: 1.25rem;
    border: 1px solid var(--border-default);
    border-radius: var(--website-radius-card);
    background: var(--surface-card);
  }

  .comic-problems h3 {
    margin: 0 0 0.6rem;
    color: var(--text-primary);
    font-size: 1rem;
  }

  .comic-problems p { margin: 0; color: var(--text-secondary); line-height: 1.65; }

  @media (max-width: 680px) {
    .comic-visual__panel { min-height: 330px; }
    .comic-balloon--speech { left: 6%; width: 62%; }
    .comic-balloon--caption { right: 5%; top: 17rem; max-width: 48%; }
    .comic-sfx { right: 10%; bottom: 5.5rem; }
    .comic-visual__legend > div { grid-template-columns: 0.65rem 1fr; }
    .comic-visual__legend > div span:last-child { grid-column: 2; }
    .comic-problems { grid-template-columns: 1fr; }
  }
</style>
