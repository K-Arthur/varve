---
import Layout from '../../layouts/Layout.astro';
import { sitePath } from '../../lib/siteUrl';
---

<Layout title="Security" description="The canonical Varve security page has moved." noindex canonicalPath="/security">
  <div class="container-custom">
    <p>
      The security policy is now at <a href={sitePath('/security')}>{sitePath('/security')}</a>.
    </p>
  </div>
</Layout>
