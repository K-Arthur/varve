{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the render spike window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
