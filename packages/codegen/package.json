{
  "name": "@strata/codegen",
  "version": "0.0.0",
  "private": true,
  "description": "IR → React/Tailwind/Flutter/SwiftUI/SVG export and .fig/.AI parsers.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@strata/scene": "workspace:*",
    "@strata/shared": "workspace:*"
  }
}
