/**
 * FocusTrap — delegates to @strata/ui FocusTrap.
 *
 * This module exists for backward compatibility with existing imports
 * within the editor package. New code should import FocusTrap directly
 * from @strata/ui.
 */
export type { FocusTrapProps } from '@strata/ui';
export { FocusTrap } from '@strata/ui';
