{
  "name": "@strata/layout",
  "version": "0.0.0",
  "private": true,
  "description": "CSS-native flex/grid/container-query layout IR mirroring the strata-layout Rust crate.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@strata/shared": "workspace:*",
    "@strata/scene": "workspace:*"
  }
}
