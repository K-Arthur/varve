{
  "name": "@varve/layout",
  "version": "0.0.0",
  "private": true,
  "description": "CSS-native flex/grid/container-query layout IR mirroring the varve-layout Rust crate.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@varve/engine": "workspace:*",
    "@varve/shared": "workspace:*",
    "@varve/scene": "workspace:*"
  },
  "license": "FSL-1.1-MIT"
}
