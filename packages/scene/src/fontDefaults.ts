/**
 * Default document typography.
 *
 * IBM Plex Sans is shipped with both the Vite browser harness and the Tauri
 * webview through @fontsource, avoiding OS-dependent fallback for new text.
 */
export { DEFAULT_ARTWORK_FONT_FAMILY } from '@strata/shared';
