/**
 * @strata/ui — Strata design-system primitives.
 *
 * Filled across tasks 0.3 (tokens + themes), 0.4 (Lucide icons), 0.5 (APG
 * component library). Re-exports are added as each lands.
 */

export const PACKAGE = '@strata/ui' as const;
